# デプロイ方針

## DBマイグレーションは自動デプロイに含めない

- **決定**: mainマージ時の自動デプロイ（`wrangler deploy`）に、DBマイグレーション（`db:migrate`）は含めない。スキーマ変更を伴うPRでは、デプロイ後に手動で `db:migrate` を実行する
- **理由**: マイグレーション実行は本番DBへの破壊的操作になり得るため
- **決定日**: 2026-08-04
